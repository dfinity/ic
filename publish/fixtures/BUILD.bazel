load("//gitlab-ci/src/artifacts:upload.bzl", "upload_fixtures")

# Add all fixtures (generated using rust_fixture_generator) that need to be 
# uploaded to the inputs
upload_fixtures(
    name = "upload_fixtures",
    inputs = ["//rs/tests:dummy_fixture"],
    out = "release_fixtures.bzl",
)
