"""
TODO
"""

load("@rules_foreign_cc//foreign_cc:meson.bzl", "meson")

# Used in make build
filegroup(
    name = "all",
    srcs = glob(["*"], exclude=["tests"]) + glob(["src/**", "scripts/**", "include/**", "tools/**", "build-aux/**", "po/**"]),
)

meson(
    name = "libvirt",
    lib_source = ":all",
    # out_bin_dir = "sbin",
    # out_binaries = ["dnsmasq"],
)
