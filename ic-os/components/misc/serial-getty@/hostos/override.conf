[Service]
# Clear existing ExecStart and replace with custom infogetty command
# The empty ExecStart= clears any existing directives from the original service
ExecStart=
# Use infogetty with limited console access instead of standard getty
ExecStart=-/opt/ic/bin/infogetty -l /etc/allow_limited_console /dev/%I