# NOTE: fsck against var_crypt races with udev, after setup-var-encryption. We
# can't delay udev as it is triggered off the device, so we delay this unit, in
# hopes that the two won't conflict.
[Service]
ExecStartPre=sleep 5
