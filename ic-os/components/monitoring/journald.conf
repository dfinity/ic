#  Use persistent logging to force storage to disk.
#
#  The default, auto, will fall back to volatile storage if the needed
#  directories don't exist. persistent creates them instead.
#
# See journald.conf(5) for details.

[Journal]
Storage=persistent
