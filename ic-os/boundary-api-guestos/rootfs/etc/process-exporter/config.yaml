# NOTE: `comm` entries must be limited to 15 chars. See https://github.com/ncabatoff/process-exporter#using-a-config-file-process-selectors.

process_names:
  - comm:
      - boundary-node-c
      - nginx
      - node_exporter
      - process-exporte
      - vector
