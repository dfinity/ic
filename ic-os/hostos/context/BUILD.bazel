package(default_visibility = ["//ic-os/hostos:__subpackages__"])

exports_files(glob(["**"]))

exports_files([
    "Dockerfile.base",
    "extra_boot_args",
    "boot_args.template",
])

filegroup(
    name = "context-files",
    srcs = [
        "Dockerfile",
        "docker-base.dev",
        "docker-base.prod",
        "packages.common",
        "packages.dev",
    ],
    tags = ["manual"],
)
