{
  "deployment": {
    "name": "{{ deployment_name }}"
  },
  "logging": {
    "hosts": "{{ logging_hosts }}"
  },
  "nns": {
    "url": "{{ nns_url }}"
  },
  "dns": {
    "name_servers": "{{ dns_name_servers }}"
  },
  "resources": {
    "memory": "{{ resources_memory }}"
  }
}
