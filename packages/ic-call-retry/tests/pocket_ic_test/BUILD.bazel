load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "retry-test",
    srcs = glob(["tests/**/*.rs"]),
    data = [
        "//packages/ic-call-retry/tests/canister:retry-test-canister.wasm.gz",
        "//rs/pocket_ic_server:pocket-ic-server",
    ],
    env = {
        "TEST_CANISTER": "$(rootpath //packages/ic-call-retry/tests/canister:retry-test-canister.wasm.gz)",
        "POCKET_IC_BIN": "$(rootpath //rs/pocket_ic_server:pocket-ic-server)",
    },
    deps = [
        # Keep sorted.
        "//packages/ic-call-chaos/call-chaos",
        "//packages/pocket-ic",
        "@crate_index//:candid",
        "@crate_index//:lazy_static",
        "@crate_index//:once_cell",
    ],
)
