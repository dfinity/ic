[package]
name = "ic-vetkd-utils"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
description.workspace = true
documentation.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zeroize = { version = "1", features = ["zeroize_derive"] }
rand = { workspace = true }
rand_chacha = { workspace = true }
lazy_static = { workspace = true }
ic_bls12_381 = { version = "0.9.2", default-features = false, features = ["pairings", "alloc", "experimental", "zeroize"] }
sha2 = { workspace = true }
sha3 = { workspace = true }
pairing = "0.23"
subtle = "2.5"
wasm-bindgen = { version = "0.2.84", optional = true }
# Enable `custom` feature of getrandom dependency. See lib.rs for more details.
getrandom = { version = "0.2", features = ["custom"], optional = true }

[dev-dependencies]
hex = { workspace = true }

[features]
default = ["js"]
js = ["dep:wasm-bindgen", "dep:getrandom"]