name: EVM RPC CI

on:
  pull_request:

jobs:
  build-evm-rpc:
    name: Build (EVM RPC fork)
    runs-on:
      group: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build Rust package
        run: cargo +nightly build --package ic-cketh-minter
