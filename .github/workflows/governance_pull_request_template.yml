# DO NOT MERGE - This line is a noop change.

name: Governance Team Pull Request Template

on:
  pull_request:
    types:
      - opened
      # DO NOT MERGE
      - synchronize

jobs:
  mainJob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        id: mainStep
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          result-encoding: string
          retries: 3
          script: |
            const pullRequestNumber = context.payload.number;

            /* DO NOT MERGE
            const oldBody = context.payload.pull_request.body;

            if (oldBody.match(/Added by GitHub Actions/)) {
              console.log("No need to modify the body, because it already has the check list.");
              return;
            }

            var newBody = oldBody ? [oldBody] : [];
            newBody = newBody.concat([
              "# Added by GitHub Actions",
              "(Or workflows, or whatever TF they are called...)",
            ]);

            newBody = newBody.join("\n\n");
            console.log("newBody = " + JSON.stringify(newBody));
            */

            /* DO NOT MERGE
            console.log("\n");
            console.log("Calling listMembersInOrg...")
            console.log("\n");
            const nnsTeamMembers = await await github.rest.teams.listMembersInOrg({
              org: "dfinity",
              team_slug: "nns-team",
            });
            console.log("\n");
            console.log("nnsTeamMemebrs = " + JSON.stringify(nnsTeamMembers));
            console.log("\n");

            const isInNnsTeam = nnsTeamMembers.data.some(member => member.login == context.actor);
            console.log("Is actor in nns-team? " + isInNNsTeam);
            if (!isInNnsTeam) {
              return;
            }
            */

            /* DO NOT MERGE
            console.log("Setting the body of pull request number " + pullRequestNumber + "...");
            await github.rest.pulls.update({
              owner: "dfinity",
              repo: "ic",
              pull_number: pullRequestNumber,
              body: newBody,
            });
            */

            await github.rest.pulls.createReview({
              owner: "dfinity",
              repo: "ic",
              pull_number: pullRequestNumber,
              body: (
                "Did you remember to update unreleased_changes.md? If so, please, " +
                "explicitly acknowledge this reminder by giving it a thumbs up reaction. " +
                "To unblock the PR, dismiss the code review by going to the bottom " +
                "of the pull request page, and use \"Done.\" as the reason."
              ),
              event: "REQUEST_CHANGES",
            });
