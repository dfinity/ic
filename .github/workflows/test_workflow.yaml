name: Test Workflow Internal
on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  ci-main-internal:
    name: CI Main
    uses: ./.github/workflows/ci-main.yml
    secrets: inherit
    with:
      runner: dind-small