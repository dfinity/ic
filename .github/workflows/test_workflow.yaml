name: Test Workflow
on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  ci-main:
    name: CI Main
    uses: ./.github/workflows/ci-main.yml
    secrets: inherit
    with:
      runner: dind-small