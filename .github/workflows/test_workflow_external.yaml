name: Test Workflow External
on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  ci-main-external:
    name: CI Main
    uses: ./.github/workflows/ci-main.yml
    secrets: inherit
    with:
      runner: ubuntu-24-large
