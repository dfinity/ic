load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

sh_binary(
    name = "afl_wrapper",
    srcs = ["afl_wrapper.sh"],
    tags = [
        "afl",
        "fuzz_test",
    ],
)

# Testing a change
