---

ic_cores: 60

ic_disk_gb: 3000

# Value is in GiB (Gibibytes)
ic_memory_gb: 490

ic_disk_path: ""

ic_media_path: ""

ic_media_branch: "master"

ic_nodes: "{{ groups['nodes'] }}"

ic_git_revision: "git-revision-is-not-set"
