load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//rs:ic-os-pkg"])

DEPENDENCIES = [
    # Keep sorted.
    "@crate_index//:anyhow",
]

rust_library(
    name = "icos_utils",
    srcs = glob(
        ["src/**/*.rs"],
    ),
    aliases = {},
    edition = "2021",
    proc_macro_deps = [],
    deps = DEPENDENCIES,
)

rust_test(
    name = "icos_utils_test",
    size = "small",
    crate = ":icos_utils",
    deps = DEPENDENCIES,
)
