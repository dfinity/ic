[package]
name = "generate_guestos_vm_config"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
askama = { workspace = true }
bootstrap_config = { path = "../bootstrap_config" }
clap = { workspace = true }
config = { path = "../config" }
config_types = { path = "../config_types" }
deterministic_ips = { path = "../deterministic_ips" }
macaddr = { workspace = true }
serde_json = { workspace = true }

[features]
dev = ["bootstrap_config/dev"]
