[package]
name = "hostos_tool"
version = "1.0.0"
edition.workspace = true

[[bin]]
name = "hostos_tool"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
config = { path = "../../config" }
config_types = { path = "../../config_types" }
deterministic_ips = { path = "../../deterministic_ips" }
manual_guestos_recovery = { path = "../manual_guestos_recovery" }
itertools = { workspace = true }
network = { path = "../../network" }
regex = { workspace = true }
utils = { path = "../../utils" }

[features]
dev = ["config/dev"]
