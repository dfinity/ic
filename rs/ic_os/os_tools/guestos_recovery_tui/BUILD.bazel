load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//rs:ic-os-pkg"])

rust_library(
    name = "guestos_recovery_tui",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "guestos_recovery_tui",
    deps = [
        "@crate_index//:anyhow",
        "@crate_index//:ratatui",
    ],
)
