load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//rs:ic-os-pkg"])

rust_library(
    name = "command_runner",
    srcs = glob(
        ["src/**/*.rs"],
    ),
    aliases = {},
    crate_name = "command_runner",
    proc_macro_deps = [
        # Keep sorted.
        "@crate_index//:async-trait",
    ],
    deps = [
        # Keep sorted.
        "@crate_index//:mockall",
        "@crate_index//:tokio",
    ],
)
