# attestation_testing

Test helpers for the `attestation` crate.

## What it provides
- Builders and fixtures for synthetic attestation packages.
- Helpers for constructing fake attestation reports with controlled contents.

Use this crate when you need to exercise attestation verification logic without
depending on real firmware or live AMD certificate material.
