load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//rs:ic-os-pkg"])

DEPENDENCIES = [
    # Keep sorted.
]

rust_library(
    name = "systemd",
    srcs = glob(
        ["src/**/*.rs"],
    ),
    aliases = {},
    crate_name = "systemd",
    edition = "2021",
    proc_macro_deps = [],
    deps = DEPENDENCIES,
)
