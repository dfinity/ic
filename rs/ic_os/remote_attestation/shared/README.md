# remote_attestation_shared

Shared types for the remote attestation service.

## What it contains
- The default TCP port used by the service.
- Rust types generated from the protobuf definition via `tonic::include_proto!`.
