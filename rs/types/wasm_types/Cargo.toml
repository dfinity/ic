[package]
name = "ic-wasm-types"
version = "0.8.0"
edition = "2018"

[dependencies]
ic-crypto-sha = { path = "../../crypto/sha" }
ic-sys = { path = "../../sys" }
ic-utils = { path = "../../utils" }
serde = { version = "1.0.99", features = ["derive"] }
