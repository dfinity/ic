load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

DEPENDENCIES = [
    # Keep sorted.
    "//rs/types/base_types",
    "@crate_index//:candid",
    "@crate_index//:serde",
    "@crate_index//:thiserror",
]

DEV_DEPENDENCIES = [
    # Keep sorted.
]

rust_library(
    name = "registry_canister_types",
    srcs = glob(["src/**"]),
    crate_name = "ic_registry_canister_types",
    version = "0.9.0",
    deps = DEPENDENCIES,
)

rust_test(
    name = "registry_canister_types_test",
    crate = ":registry_canister_types",
)
