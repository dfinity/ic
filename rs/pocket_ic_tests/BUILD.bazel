load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

DEPENDENCIES = [
    # Keep sorted.
    "//packages/pocket-ic",
    "//rs/protobuf",
    "//rs/types/error_types",
    "//rs/types/management_canister_types",
    "//rs/types/types",
    "@crate_index//:candid",
    "@crate_index//:wat",
]

rust_library(
    name = "pocket_ic_tests",
    testonly = True,
    srcs = [
        "src/lib.rs",
    ],
    crate_name = "ic_pocket_ic_tests",
    version = "0.9.0",
    deps = DEPENDENCIES,
)
