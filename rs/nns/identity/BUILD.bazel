load("//bazel:canisters.bzl", "optimized_canister", "rust_canister")

package(default_visibility = ["//visibility:public"])

DEPENDENCIES = []

MACRO_DEPENDENCIES = []

ALIASES = {}

rust_canister(
    name = "identity-canister-raw",
    srcs = ["canister/canister.rs"],
    aliases = ALIASES,
    proc_macro_deps = MACRO_DEPENDENCIES,
    deps = DEPENDENCIES,
)

optimized_canister(
    name = "identity-canister",
    wasm = ":identity-canister-raw",
)
