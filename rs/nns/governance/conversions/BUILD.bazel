load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "conversions",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "ic_nns_governance_conversions",
    deps = [
        # Keep sorted.
        "//rs/nns/governance/api",
        "//rs/protobuf",
    ],
)
