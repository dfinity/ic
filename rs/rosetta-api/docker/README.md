# Dockerfiles for building things locally

The `Dockerfile`s in this directory serve as executable and
reproducible documentation for how to build `ic-rosetta-api` and
`ledger-canister.wasm` locally.
