load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = [
    "//rs/http_endpoints/public:__subpackages__",
    "//rs/tests/networking:__subpackages__",
])

rust_library(
    name = "test_agent",
    testonly = True,
    srcs = glob(["src/**"]),
    crate_name = "ic_http_endpoints_test_agent",
    version = "0.9.0",
    deps = [
        # Keep sorted.
        "//rs/crypto/tree_hash",
        "//rs/http_endpoints/public",
        "//rs/types/types",
        "@crate_index//:reqwest",
        "@crate_index//:serde_cbor",
        "@crate_index//:slog",
        "@crate_index//:tokio",
        "@crate_index//:url",
    ],
)
