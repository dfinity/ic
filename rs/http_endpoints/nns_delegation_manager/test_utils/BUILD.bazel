load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = [
    "//rs/http_endpoints/nns_delegation_manager:__subpackages__",
])

rust_library(
    name = "test_utils",
    testonly = True,
    srcs = glob(["src/**/*.rs"]),
    crate_name = "ic_nns_delegation_manager_test_utils",
    deps = [
        # Keep sorted.
        "//rs/certification/test-utils",
        "//rs/types/types",
        "@crate_index//:rand",
    ],
)
