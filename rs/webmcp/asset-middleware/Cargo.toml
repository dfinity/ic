[package]
name = "ic-webmcp-asset-middleware"
version = "0.1.0"
edition = "2021"
description = "Middleware helpers for serving WebMCP manifests from IC asset canisters"
license = "Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = "1"
