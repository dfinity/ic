load("@rules_rust//rust:defs.bzl", "rust_binary")
load(
    "//rs/tests:common.bzl",
    "DEFAULT_VCPUS_PER_VM",
    "IC_GATEWAY_RUNTIME_DEPS",
    "MAINNET_ENV",
    "MAINNET_NNS_SYSTEM_TEST_RUNTIME_DEPS",
    "MIN_LOCAL_CPUS",
    "NNS_CANISTER_RUNTIME_DEPS",
    "SNS_CANISTER_RUNTIME_DEPS",
)
load("//rs/tests:system_tests.bzl", "system_test", "system_test_nns")

package(default_visibility = ["//rs:system-tests-pkg"])

# the "_test" postfixes in names are dropped on purpose since this target is meant for interactive use.

system_test(
    name = "single_large_node",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 64,  # 1 System node @ 64 vCPUs (subnet resource override).
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    visibility = ["//visibility:public"],  # public because part of top-level manual test suite
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test(
    name = "single_app_large_node",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 64,  # 1 Application node @ 64 vCPUs (subnet resource override).
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "single_app_large_node_with_nns",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + DEFAULT_VCPUS_PER_VM + 64,  # 1 System node @ 6 vCPUs + 1 Application node @ 64 vCPUs (subnet resource override).
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test(
    name = "single_app_small_node",
    cpus = MIN_LOCAL_CPUS + 16,  # 1 Application node @ 16 vCPUs (subnet resource override).
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "small",
    cpus = MIN_LOCAL_CPUS + 5 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 Application + 1 unassigned + 1 API boundary node + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "small_bitcoin",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 64 + 2 * DEFAULT_VCPUS_PER_VM,  # 1 System node @ 64 vCPUs (subnet override) + 1 API boundary node + 1 ic-gateway VM @ 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "small_high_perf",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 64 + 3 * DEFAULT_VCPUS_PER_VM,  # 1 Application node @ 64 vCPUs (subnet override) + 1 System + 1 API boundary node + 1 ic-gateway VM @ 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test(
    name = "from_config",
    cpus = MIN_LOCAL_CPUS,  # TODO: determine vCPUs used by the setup function (topology is driven entirely by the IC_CONFIG env var).
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = NNS_CANISTER_RUNTIME_DEPS,
    deps = [
        "//rs/tests/dre/utils:os_qualification_utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
        "@crate_index//:serde_json",
    ],
)

system_test_nns(
    name = "small_nns",
    cpus = MIN_LOCAL_CPUS + 5 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 Application + 1 API boundary node + 1 unassigned + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | {
        "IC_ICRC1_LEDGER_WASM_PATH": "//rs/ledger_suite/icrc1/ledger:ledger_canister",
        "II_BACKEND_WASM_PATH": "@mainnet_canisters//:internet_identity_backend.wasm.gz",
        "II_FRONTEND_WASM_PATH": "@mainnet_canisters//:internet_identity_frontend.wasm.gz",
        "NNS_DAPP_WASM_PATH": "@nns_dapp_canister//file",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "small_with_query_stats",
    cpus = MIN_LOCAL_CPUS + 5 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 Application + 1 API boundary node + 1 unassigned + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | {
        "IC_ICRC1_LEDGER_WASM_PATH": "//rs/ledger_suite/icrc1/ledger:ledger_canister",
        "NNS_DAPP_WASM_PATH": "@nns_dapp_canister//file",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "sns_testing",
    cpus = MIN_LOCAL_CPUS + 6 * DEFAULT_VCPUS_PER_VM,  # 1 System + 2 Application + 1 API boundary node + 1 unassigned + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | SNS_CANISTER_RUNTIME_DEPS | {
        "II_BACKEND_WASM_PATH": "@mainnet_canisters//:internet_identity_backend.wasm.gz",
        "II_FRONTEND_WASM_PATH": "@mainnet_canisters//:internet_identity_frontend.wasm.gz",
        "NNS_DAPP_WASM_PATH": "@nns_dapp_canister//file",
        "SNS_AGGREGATOR_WASM_PATH": "@sns_aggregator//file",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "cloud_engine",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 23 * DEFAULT_VCPUS_PER_VM,  # 1 System + 20 unassigned nodes (NUM_UNASSIGNED_NODES default) + 1 API boundary node + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | {
        "II_BACKEND_WASM_PATH": "@mainnet_canisters//:internet_identity_backend.wasm.gz",
        "II_FRONTEND_WASM_PATH": "@mainnet_canisters//:internet_identity_frontend.wasm.gz",
        "NNS_DAPP_WASM_PATH": "@mainnet_canisters//:nns_dapp_test.wasm.gz",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/protobuf",
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "//rs/types/types",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "medium",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 11 * DEFAULT_VCPUS_PER_VM,  # 4 System + 4 Application + 1 unassigned + 1 API boundary node + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "large",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 10 * 64 + DEFAULT_VCPUS_PER_VM,  # 9 subnet nodes (4 System + 4 + 1 Application) + 1 API boundary node @ 64 vCPUs (IC-level override) + 1 ic-gateway VM @ 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | SNS_CANISTER_RUNTIME_DEPS | {
        "II_BACKEND_WASM_PATH": "@mainnet_canisters//:internet_identity_backend.wasm.gz",
        "II_FRONTEND_WASM_PATH": "@mainnet_canisters//:internet_identity_frontend.wasm.gz",
        "NNS_DAPP_WASM_PATH": "@nns_dapp_canister//file",
        "SNS_AGGREGATOR_WASM_PATH": "@sns_aggregator//file",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "@crate_index//:anyhow",
    ],
)

system_test_nns(
    name = "io_perf_benchmark",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 64 + 3 * DEFAULT_VCPUS_PER_VM,  # 1 Application node @ 64 vCPUs (subnet override, default 1 perf host) + 1 System + 1 API boundary node + 1 ic-gateway VM @ 6 vCPUs.
    enable_metrics = True,
    prometheus_vm_required_host_features = ["performance"],
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    vm_allocation_mode = "performanceOptimizedAllocation",
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "baremetal_app_subnet",
    cpus = MIN_LOCAL_CPUS + 3 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 API boundary node + 1 ic-gateway VM @ 6 vCPUs (the bare-metal node is external hardware, not a Farm VM).
    enable_metrics = True,
    env = MAINNET_ENV,
    env_inherit = [
        "BARE_METAL_HOST_SECRETS",
        "TRUSTED_EXECUTION_ENVIRONMENT",
    ],
    guestos = True,
    hostos = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | {
        "NODE_OPERATOR_PRIV_KEY_PATH": "//ic-os/setupos:config/node_operator_private_key.pem",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_type",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nested",
        "//rs/tests/nns/nns_dapp",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "src_testing",
    backend = "farm",  # Too big for the "local" backend.
    cpus = MIN_LOCAL_CPUS + 36 * DEFAULT_VCPUS_PER_VM,  # 32 Application + 2 System + 1 API boundary node + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | SNS_CANISTER_RUNTIME_DEPS | {
        "XRC_WASM_PATH": "//rs/rust_canisters/xrc_mock:xrc_mock_canister",
        "II_BACKEND_WASM_PATH": "@mainnet_canisters//:internet_identity_backend.wasm.gz",
        "II_FRONTEND_WASM_PATH": "@mainnet_canisters//:internet_identity_frontend.wasm.gz",
        "NNS_DAPP_WASM_PATH": "@nns_dapp_canister//file",
        "SNS_AGGREGATOR_WASM_PATH": "@sns_aggregator//file",
        "SUBNET_RENTAL_WASM_PATH": "@mainnet_canisters//:subnet_rental.wasm.gz",
    },
    deps = [
        # Keep sorted.
        "//rs/registry/subnet_features",
        "//rs/registry/subnet_type",
        "//rs/rust_canisters/xrc_mock",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nns/nns_dapp",
        "//rs/types/base_types",
        "@crate_index//:anyhow",
        "@crate_index//:candid",
        "@crate_index//:ic-xrc-types",
    ],
)

rust_binary(
    name = "nns_recovery_testnet_bin",
    testonly = True,
    srcs = ["nns_recovery.rs"],
    deps = [
        # Keep sorted.
        "//rs/limits",
        "//rs/tests/consensus/subnet_recovery:lib",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/nested/nns_recovery:common",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "nns_recovery",
    cpus = MIN_LOCAL_CPUS + 3 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 API boundary node + 1 ic-gateway VM @ 6 vCPUs (SUBNET_SIZE nested HostOS nodes, default 0, not included).
    enable_metrics = True,
    env = {
        "USE_MAINNET_STATE": "false",
    },
    setupos = True,
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    test_driver_target = ":nns_recovery_testnet_bin",
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS,
)

system_test_nns(
    name = "mainnet_nns_recovery",
    cpus = MIN_LOCAL_CPUS + 5 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 API boundary node + 1 unassigned + 1 aux UVM + 1 ic-gateway VM, all 6 vCPUs (SUBNET_SIZE nested HostOS nodes, default 0, not included).
    enable_metrics = True,
    env = MAINNET_ENV | {
        "USE_MAINNET_STATE": "true",
    },
    env_inherit = ["SSH_AUTH_SOCK"],
    guestos = "mainnet_nns_dev",  # necessary for configuring NNS public key
    prometheus_vm_resources = {
        "vcpus": 32,
        "memory_kibibytes": 125000000,
        "boot_image_minimal_size_gibibytes": 500,
    },
    setupos = "mainnet_nns_dev",
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    test_driver_target = ":nns_recovery_testnet_bin",
    runtime_deps = MAINNET_NNS_SYSTEM_TEST_RUNTIME_DEPS,
)

system_test_nns(
    name = "mainnet_nns_state",
    cpus = MIN_LOCAL_CPUS + 5 * DEFAULT_VCPUS_PER_VM,  # 1 System + 1 API boundary node + 1 unassigned + 1 aux UVM + 1 ic-gateway VM, all 6 vCPUs.
    enable_metrics = True,
    env = MAINNET_ENV,
    env_inherit = ["SSH_AUTH_SOCK"],
    guestos = "mainnet_nns",
    # Requires more resources to scrape mainnet topology
    prometheus_vm_resources = {
        "vcpus": 32,
        "memory_kibibytes": 125000000,
        "boot_image_minimal_size_gibibytes": 500,
    },
    tags = [
        "dynamic_testnet",
        "manual",
    ],
    runtime_deps = MAINNET_NNS_SYSTEM_TEST_RUNTIME_DEPS,
    deps = [
        # Keep sorted.
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/tests/testnets/mainnet_nns:lib",
        "@crate_index//:anyhow",
    ],
)
