load("//rs/tests:common.bzl", "COUNTER_CANISTER_RUNTIME_DEPS", "DEFAULT_VCPUS_PER_VM", "MAINNET_ENV", "MAINNET_NNS_CANISTER_RUNTIME_DEPS", "MIN_LOCAL_CPUS", "XNET_TEST_CANISTER_RUNTIME_DEPS")
load("//rs/tests:system_tests.bzl", "system_test")

package(default_visibility = ["//rs:system-tests-pkg"])

system_test(
    name = "guest_os_qualification",
    cpus = MIN_LOCAL_CPUS + 18 * DEFAULT_VCPUS_PER_VM,  # 12 subnet + 4 unassigned + 2 API boundary node VMs = 18 IC Node VMs * 6 vCPUs.
    env = MAINNET_ENV,
    tags = [
        "colocate",
        "manual",
    ],
    runtime_deps = XNET_TEST_CANISTER_RUNTIME_DEPS | MAINNET_NNS_CANISTER_RUNTIME_DEPS | COUNTER_CANISTER_RUNTIME_DEPS | {
        "IC_VERSION_FILE": "//bazel:version.txt",
    },
    deps = [
        # Keep sorted
        "//rs/protobuf",
        "//rs/registry/subnet_type",
        "//rs/tests/dre/utils:os_qualification_utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/types/types",
        "@crate_index//:anyhow",
        "@crate_index//:tokio",
    ],
)
