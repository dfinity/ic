load("//rs/tests:common.bzl", "COUNTER_CANISTER_RUNTIME_DEPS", "MAINNET_ENV", "MAINNET_NNS_CANISTER_RUNTIME_DEPS", "XNET_TEST_CANISTER_DATA")
load("//rs/tests:system_tests.bzl", "system_test")

package(default_visibility = ["//rs:system-tests-pkg"])

system_test(
    name = "guest_os_qualification",
    data = XNET_TEST_CANISTER_DATA,
    env = MAINNET_ENV,
    tags = [
        "colocate",
        "manual",
    ],
    runtime_deps = MAINNET_NNS_CANISTER_RUNTIME_DEPS | COUNTER_CANISTER_RUNTIME_DEPS | {
        "XNET_TEST_CANISTER_WASM_PATH": "//rs/rust_canisters/xnet_test:xnet-test-canister",
        "ENV_DEPS__IC_VERSION_FILE": "//bazel:version.txt",
    },
    deps = [
        # Keep sorted
        "//rs/protobuf",
        "//rs/registry/subnet_type",
        "//rs/tests/dre/utils:os_qualification_utils",
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/types/types",
        "@crate_index//:anyhow",
        "@crate_index//:tokio",
    ],
)
