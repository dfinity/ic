load("@rules_motoko//motoko:defs.bzl", "motoko_binary")
load("@rules_rust//rust:defs.bzl", "rust_library")
load("//rs/tests:common.bzl", "DEFAULT_VCPUS_PER_VM", "IC_GATEWAY_RUNTIME_DEPS", "MIN_LOCAL_CPUS")
load("//rs/tests:system_tests.bzl", "system_test", "system_test_nns")

package(default_visibility = ["//rs:system-tests-pkg"])

# Just for testing if the Motoko code is correct.
motoko_binary(
    name = "counter_canister",
    entry = "canisters/counter.mo",
)

rust_library(
    name = "sdk-system-tests",
    testonly = True,
    srcs = glob(["src/**/*.rs"]),
    compile_data = [
        "canisters/counter.mo",
    ],
    crate_name = "sdk_system_tests",
    deps = [
        # Keep sorted.
        "//rs/tests/driver:ic-system-test-driver",
        "//rs/types/types",
        "@crate_index//:backon",
        "@crate_index//:candid",
        "@crate_index//:reqwest",
        "@crate_index//:serde_json",
        "@crate_index//:slog",
    ],
)

system_test_nns(
    name = "dfx_smoke_test",
    cpus = MIN_LOCAL_CPUS + 3 * DEFAULT_VCPUS_PER_VM,  # 1 IC Node VM + 1 API Boundary Node VM + 1 IC Gateway VM, each 6 vCPUs.
    runtime_deps = IC_GATEWAY_RUNTIME_DEPS | {
        "DFX_PATH": "//:dfx",
    },
    deps = [
        # Keep sorted.
        "sdk-system-tests",
        "//rs/registry/subnet_type",
        "//rs/tests/consensus/utils",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)

system_test(
    name = "dfx_check_old_wallet_version_test",
    cpus = MIN_LOCAL_CPUS + 1 * DEFAULT_VCPUS_PER_VM,  # 1 IC Node VM * 6 vCPUs.
    runtime_deps = {
        "WALLET_CANISTER_0_7_2_WASM": "@wallet_canister_0.7.2//file",
        "DFX_PATH": "//:dfx",
    },
    deps = [
        # Keep sorted.
        "sdk-system-tests",
        "//rs/registry/subnet_type",
        "//rs/tests/driver:ic-system-test-driver",
        "@crate_index//:anyhow",
        "@crate_index//:slog",
    ],
)
