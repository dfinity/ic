# ic-utils-thousands

A tiny, dependency-free helper for formatting integers with an underscore between every group of three digits (e.g. `1234567` becomes `1_234_567`), exposing a single `separate_with_underscores` function.
