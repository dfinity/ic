load("@rules_rust//rust:defs.bzl", "rust_library")
load("//bazel:canisters.bzl", "rust_canister")

package(default_visibility = ["//visibility:public"])

DEPENDENCIES = [
    # Keep sorted.
    "//rs/rust_canisters/dfn_core",
    "//rs/types/base_types",
    "//rs/types/types",
    "@crate_index//:candid",
    "@crate_index//:ic-cdk",
    "@crate_index//:serde",
]

MACRO_DEPENDENCIES = [
    # Keep sorted.
    "@crate_index//:ic-cdk-macros",
]

rust_library(
    name = "subnet_memory_test",
    srcs = ["src/lib.rs"],
    crate_name = "subnet_memory_test",
    version = "0.1.0",
    deps = DEPENDENCIES,
)

rust_canister(
    name = "subnet-memory-test-canister",
    srcs = ["src/main.rs"],
    proc_macro_deps = MACRO_DEPENDENCIES,
    service_file = ":subnet_memory_test.did",
    deps = DEPENDENCIES + [":subnet_memory_test"],
)
