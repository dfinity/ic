load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "logging",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "ic_logging",
    version = "0.9.0",
    deps = [
        "//rs/config",
        "//rs/types/types",
        "@crate_index//:tracing",
        "@crate_index//:tracing-appender",
        "@crate_index//:tracing-subscriber",
    ],
)
