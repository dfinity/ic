load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "resource_limits",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "ic_registry_resource_limits",
    version = "0.9.0",
    deps = [
        # Keep sorted.
        "//rs/protobuf",
        "@crate_index//:candid",
        "@crate_index//:serde",
    ],
)
